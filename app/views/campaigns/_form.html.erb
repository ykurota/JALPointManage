<%= form_for(campaign) do |f| %>
  <% if campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

      <ul>
      <% campaign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ctype %>
    <%= f.text_field :ctype %>
  </div>

  <div class="field">
    <%= f.label :ctypedetail %>
    <%= f.text_field :ctypedetail %>
  </div>

  <div class="field">
    <%= f.label :bway %>
    <%= f.text_field :bway %>
  </div>

  <div class="field">
    <%= f.label :bpoint %>
    <%= f.text_field :bpoint %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
